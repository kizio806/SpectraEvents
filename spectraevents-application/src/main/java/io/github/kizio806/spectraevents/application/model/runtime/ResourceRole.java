package io.github.kizio806.spectraevents.application.model.runtime;

/** Resource role tag for native entities spawned by 3D models. */
public enum ResourceRole {
  VISUAL_PART,
  INTERACTION_HITBOX
}
