name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  JAVA_VERSION: '25'

jobs:
  build-and-publish:
    name: Build & Publish Release
    runs-on: ubuntu-24.04
    timeout-minutes: 40

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Read project metadata
        id: metadata
        run: |
          VERSION=$(grep '^spectraevents.version=' gradle.properties | cut -d'=' -f2 | tr -d ' \r\n')
          GROUP=$(grep '^spectraevents.group=' gradle.properties | cut -d'=' -f2 | tr -d ' \r\n')
          echo "GRADLE_VERSION=$VERSION" >> $GITHUB_ENV
          echo "GRADLE_GROUP=$GROUP" >> $GITHUB_ENV
          echo "Read version '$VERSION' and group '$GROUP' from gradle.properties."

      - name: Validate tag version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          if [[ "$TAG_NAME" != v* ]]; then
            echo "::error::Tag name '$TAG_NAME' must start with 'v'!"
            exit 1
          fi
          TAG_VERSION="${TAG_NAME#v}"
          if [ "$TAG_VERSION" != "$GRADLE_VERSION" ]; then
            echo "::error::Tag version '$TAG_VERSION' does not match Gradle project version '$GRADLE_VERSION'!"
            exit 1
          fi
          echo "Tag version '$TAG_VERSION' successfully validated against Gradle version '$GRADLE_VERSION'."

      - name: Determine release type
        run: |
          if [[ "$GRADLE_VERSION" == *"-alpha"* ]]; then
            echo "RELEASE_TYPE=alpha" >> $GITHUB_ENV
            echo "MODRINTH_TYPE=alpha" >> $GITHUB_ENV
            echo "IS_PRERELEASE=true" >> $GITHUB_ENV
          elif [[ "$GRADLE_VERSION" == *"-beta"* ]] || [[ "$GRADLE_VERSION" == *"-rc"* ]]; then
            echo "RELEASE_TYPE=beta" >> $GITHUB_ENV
            echo "MODRINTH_TYPE=beta" >> $GITHUB_ENV
            echo "IS_PRERELEASE=true" >> $GITHUB_ENV
          else
            echo "RELEASE_TYPE=release" >> $GITHUB_ENV
            echo "MODRINTH_TYPE=release" >> $GITHUB_ENV
            echo "IS_PRERELEASE=false" >> $GITHUB_ENV
          fi

      - name: Execute full quality gate & build
        run: ./gradlew --no-daemon --stacktrace clean check build

      - name: Discover and verify final distribution artifacts
        id: artifacts
        run: |
          for platform in paper spigot sponge; do
            LIBS_DIR="distributions/$platform/build/libs"
            JARS=($(find "$LIBS_DIR" -name "SpectraEvents-*.jar" ! -name "*-sources.jar"))
            if [ ${#JARS[@]} -eq 0 ]; then
              echo "::error::No distribution JAR found for $platform in $LIBS_DIR!"
              exit 1
            fi
            if [ ${#JARS[@]} -gt 1 ]; then
              echo "::error::Multiple distribution JARs found for $platform: ${JARS[*]}"
              exit 1
            fi
            JAR_PATH="${JARS[0]}"
            echo "Verified distribution artifact for $platform: $JAR_PATH"
          done

      - name: Generate Release Notes
        run: |
          cat << EOF > release-notes.md
          # SpectraEvents v${GRADLE_VERSION}

          - **Minecraft Version Compatibility Band:** 26.1 – 26.3
          - **Platforms:** Paper, Purpur, Folia, Spigot, Sponge
          - **Java Runtime Requirement:** 25

          EOF

          if [ "$IS_PRERELEASE" = "true" ]; then
            echo "> **Notice:** This is a pre-release version for testing." >> release-notes.md
            echo "" >> release-notes.md
          fi

          if [ -f CHANGELOG.md ]; then
            CHANGELOG_SECTION=$(awk -v ver="${GRADLE_VERSION}" '
              $0 ~ "^## \\[" ver "\\]" { flag=1; next }
              $0 ~ "^## \\[" { flag=0 }
              flag { print }
            ' CHANGELOG.md)

            if [ -n "$CHANGELOG_SECTION" ]; then
              echo "$CHANGELOG_SECTION" >> release-notes.md
            else
              echo "### Changes" >> release-notes.md
              git log -n 10 --oneline >> release-notes.md
            fi
          else
            echo "### Changes" >> release-notes.md
            git log -n 10 --oneline >> release-notes.md
          fi

      - name: Compute and verify SHA-256 checksums
        run: |
          mkdir -p release-artifacts
          for platform in paper spigot sponge; do
            cp distributions/$platform/build/libs/SpectraEvents-*.jar release-artifacts/
          done

          cd release-artifacts
          sha256sum SpectraEvents-*.jar > SHA256SUMS.txt
          cat SHA256SUMS.txt
          sha256sum -c SHA256SUMS.txt
          cd ..

      - name: Upload Release Artifacts to Actions
        uses: actions/upload-artifact@v7
        with:
          name: spectraevents-release-artifacts
          path: |
            release-artifacts/SpectraEvents-*.jar
            release-artifacts/SHA256SUMS.txt
            release-notes.md

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          name: SpectraEvents v${{ env.GRADLE_VERSION }}
          body_path: release-notes.md
          files: |
            release-artifacts/SpectraEvents-*.jar
            release-artifacts/SHA256SUMS.txt
          prerelease: false
          make_latest: true

      - name: Publish Paper Family to Modrinth
        if: startsWith(github.ref, 'refs/tags/')
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ secrets.MODRINTH_PROJECT_ID || vars.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          name: SpectraEvents ${{ env.GRADLE_VERSION }} — Paper Family
          version: ${{ env.GRADLE_VERSION }}-paper
          version-type: ${{ env.MODRINTH_TYPE }}
          game-versions: |
            26.1
            26.1.1
            26.1.2
            26.2
            26.3
          loaders: |
            paper
            purpur
            folia
          java: 25
          environment: server
          files: release-artifacts/SpectraEvents-*-paper.jar
          changelog-file: release-notes.md

      - name: Publish Spigot Family to Modrinth
        if: startsWith(github.ref, 'refs/tags/')
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ secrets.MODRINTH_PROJECT_ID || vars.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          name: SpectraEvents ${{ env.GRADLE_VERSION }} — Spigot Family
          version: ${{ env.GRADLE_VERSION }}-spigot
          version-type: ${{ env.MODRINTH_TYPE }}
          game-versions: |
            26.1
            26.1.1
            26.1.2
            26.2
            26.3
          loaders: |
            spigot
            bukkit
          java: 25
          environment: server
          files: release-artifacts/SpectraEvents-*-spigot.jar
          changelog-file: release-notes.md

      - name: Publish Sponge Family to Modrinth
        if: startsWith(github.ref, 'refs/tags/')
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ secrets.MODRINTH_PROJECT_ID || vars.MODRINTH_PROJECT_ID }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          name: SpectraEvents ${{ env.GRADLE_VERSION }} — Sponge
          version: ${{ env.GRADLE_VERSION }}-sponge
          version-type: ${{ env.MODRINTH_TYPE }}
          game-versions: |
            26.1
            26.1.1
            26.1.2
            26.2
          loaders: |
            sponge
          java: 25
          environment: server
          files: release-artifacts/SpectraEvents-*-sponge.jar
          changelog-file: release-notes.md

      - name: Generate Release Summary
        if: always()
        run: |
          echo "# SpectraEvents Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "| --- | --- |" >> $GITHUB_STEP_SUMMARY
          echo "| **Version** | \`${{ env.GRADLE_VERSION }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Release Type** | \`${{ env.RELEASE_TYPE }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Target Minecraft** | \`26.2\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Platform** | \`Paper / Spigot / Sponge\` |" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "| **GitHub Release** | Published |" >> $GITHUB_STEP_SUMMARY
            echo "| **Modrinth** | Published |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **GitHub Release** | Skipped (Not a tag build) |" >> $GITHUB_STEP_SUMMARY
            echo "| **Modrinth** | Skipped (Not a tag build) |" >> $GITHUB_STEP_SUMMARY
          fi
