# SpectraEvents

The canonical repository instructions are in `AGENTS.md`. Before substantial work, read that file,
the mandatory documents under `docs/ai/`, the current `PROJECT_STATE.md`, and the relevant code and
tests. This file does not define separate architectural decisions.
